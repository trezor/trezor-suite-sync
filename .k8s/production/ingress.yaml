apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: suite-sync-prod
  namespace: suite-sync
  annotations:
    ingress.health.argocd.io/ip-not-requested: "true"
    nginx.org/path-regex: "case_sensitive"
    nginx.org/rewrite-target: "/$2"
    nginx.org/proxy-read-timeout: "100s"
    nginx.org/proxy-send-timeout: "10s"
    nginx.org/proxy-connect-timeout: "10s"
spec:
  ingressClassName: nginx
  rules:
    - host: suite-sync.trezor.io
      http:
        paths:
          - path: /evolu(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: evolu-relay
                port:
                  number: 4000
          - path: /quota-manager(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: quota-manager
                port:
                  number: 4001
