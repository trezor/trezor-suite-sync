apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: suite-sync-dev

resources:
  - ../base/namespace.yaml
  - evolu-relay.yaml
  - quota-manager.yaml
  - ingress.yaml

images:
  - name: ghcr.io/trezor/suite-sync
    newTag: main
